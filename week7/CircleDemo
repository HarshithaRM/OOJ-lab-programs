/*2. Develop a Java program which has the (only) class CircleDemo that has members-
radius, area and perimeter. Include methods to do the following.

a. accept the radius from the user
b. find the area of the circle
c. find the perimeter of the circle
d. Display all the details*/
import java.util.Scanner;
class  CircleDemo
{
	static private double r,a,p;

	static void accept()
	{
		Scanner sc=new Scanner(System.in);
		System.out.println("enter radius of circle: ");
		r=sc.nextDouble();
	}
	static void area()
	{
		a=3.14*r*r;
	}
	static void peri()
	{
		p=2*3.14*r;
	}

	static void display()
	{
	   System.out.println("area="+a);
	   System.out.println("perimeter="+r);
	}

    public static void main(String[] args) 
	{
		accept();
		area();
		peri();
		display();

	}
}
